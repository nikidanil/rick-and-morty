# 🚀 Проект: Rick and Morty Wiki

## 🎯 Цель репозитория

Цель данного проекта — **практическое применение возможностей React Router v6** в современном React-приложении (React 19).  
Проект реализует простую, но полноценную навигационную структуру с вложенными маршрутами, динамическими параметрами, обработкой 404 и синхронизацией состояния с URL.

Кроме навигации, проект демонстрирует:

-   Работу с API (через `json-server`)
-   Управление состоянием авторизации через **React Context (React 19)**
-   Приватный роутинг с защитой доступа к страницам
-   Повторное использование компонентов и хуков
-   Корректную типизацию (TypeScript)
-   Чистую архитектуру компонентов

---

## 🧩 Решаемая задача

### Проблема

В реальных приложениях часто требуется:

-   Переходить между страницами с сохранением состояния
-   Делать часть UI общей (например, шапку)
-   Показывать детальную информацию по ID (`/heroes/1`)
-   Поддерживать "живые" ссылки (например, с сортировкой в URL)
-   Обрабатывать несуществующие маршруты
-   **Ограничивать доступ к определённым страницам без авторизации**
-   **Поддерживать состояние пользователя между действиями**

### Решение

Проект решает эти задачи с помощью **React Router v6**, реализуя:

-   Вложенную навигацию через `<Outlet />`
-   Динамические маршруты (`/:id`)
-   Управление сортировкой через query-параметры (`?sort=asc`)
-   Единый `MainLayout` для общей обёртки
-   Страницу 404 для несуществующих путей
-   **React Context API (React 19)** — для управления состоянием авторизации
-   **`use(context)`** — новый способ чтения контекста в React 19
-   **`<AuthContext value={...}>`** — новое синтаксическое поведение (вместо `.Provider`)
-   **`PrivateRoute`** — компонент-обёртка для защиты доступа
-   **`useAuth`** — кастомный хук для доступа к данным пользователя
-   **`Login`-страница** — с перенаправлением после входа на запрашиваемый маршрут

---

## 🗂️ Структура репозитория

-   src/
    -   components/
        -   MainLayout/
        -   SortPanel/
        -   PrivateRoute/ — защищённый маршрут
        -   common/
    -   hooks/
        -   useSortParams/ — работа с query-параметрами сортировки
        -   useAuth/ — доступ к данным пользователя
    -   pages/
        -   HeroesPage/
        -   HeroPage/
        -   LocationsPage/
        -   EpisodesPage/
        -   HomePage/
        -   Login/ — страница входа
        -   NotFoundPage/
    -   constants/
    -   context/
        -   authContext.ts — контекст авторизации
        -   AuthProvider.tsx — провайдер состояния
    -   App.tsx
    -   db.json

### 🔗 Как всё взаимодействует

-   **`App.tsx`** — корневой роутер:

    -   Все защищённые страницы обёрнуты в `<PrivateRoute>`
    -   `AuthProvider` охватывает всё приложение
    -   Все страницы вложены в `MainLayout`
    -   Используются вложенные маршруты (`/heroes`, `/heroes/:id`)
    -   `*` — ловит все неизвестные пути → `NotFoundPage`

-   **`useSortParams`** — хук:

    -   Считывает `?sort=asc` из URL
    -   При изменении — обновляет URL без дублирования истории
    -   Удаляет параметр, если `desc` (значение по умолчанию)

-   **`SortPanel`** — универсальный компонент:

    -   Принимает `label`, `sortOrder`, `setSortOrder`
    -   Используется на всех страницах с сортировкой

-   **Страницы (`HeroesPage`, `LocationsPage`, `EpisodesPage`)**:

    -   Загружают данные с `json-server`
    -   Используют один и тот же хук `useSortParams`
    -   Сортируют по полю `created`

-   **`HeroPage`, `LocationPage`, `EpisodePage`**:

    -   Используют `useParams()` для получения `id`
    -   Загружают детали по ID
    -   Отображают через `InfoCard` и `PhotoCard`

-   **`AuthProvider`** — оборачивает всё приложение:

    -   Хранит `user` в состоянии
    -   Предоставляет `signIn` и `signOut`
    -   Использует новое поведение React 19: `<AuthContext value={...}>`

-   **`useAuth`** — хук:

    -   Использует `use(AuthContext)` — рекомендованный способ в React 19
    -   Возвращает текущее состояние пользователя и методы

-   **`PrivateRoute`**:

    -   Проверяет `auth.user === null`
    -   Если не авторизован — редиректит на `/login` с сохранением цели (`state.from`)
    -   Используется для защиты `/heroes`, `/locations`, `/episodes` и их детальных страниц

-   **`Login`**:
    -   Позволяет ввести логин
    -   При входе вызывает `auth.signIn(login, callback)`
    -   Перенаправляет на предыдущую страницу (`from`) или на главную
    -   Использует `useLocation()` для получения целевой страницы

---

## 🛠️ Как запустить и использовать

### 1. Установка зависимостей

```bash
npm install
```

### 2. Запуск сервера и приложения

```bash
npm start
```

Это запустит:

-   json-server на http://localhost:3000 (API)
-   vite на http://localhost:5173 (фронтенд)

### 3. Доступные маршруты

| Маршрут         | Тип доступа | Описание                         |
| --------------- | ----------- | -------------------------------- |
| /               | Публичный   | Главная страница                 |
| /login          | Публичный   | Страница входа                   |
| /heroes         | Приватный   | Список персонажей (требует вход) |
| /heroes/1       | Приватный   | Детали персонажа с ID=1          |
| /locations      | Приватный   | Список локаций                   |
| /locations/1    | Приватный   | Детали локации                   |
| /episodes       | Приватный   | Список эпизодов                  |
| /episodes/1     | Приватный   | Детали эпизода                   |
| /any-wrong-path | Публичный   | Страница 404                     |

### 4. Функциональность

**Авторизация:**

-   При попытке зайти на `/heroes` без входа — редирект на `/login`
-   После входа — автоматический возврат на запрашиваемую страницу
-   Данные пользователя хранятся в контексте (не в localStorage — для простоты)

**Сортировка:**

-   На страницах списков — можно выбрать "Сначала новые" / "Сначала старые"
-   Параметр ?sort=asc сохраняется в URL
-   При desc — параметр удаляется (чистота URL)

**Навигация:**

-   Через панель в MainLayout
-   Поддержка кнопки "Назад" в браузере
-   Активные ссылки подсвечиваются

**404:**

-   Любая несуществующая страница → NotFoundPage с цитатой Рика

## 🔧 Технологии

-   **React 19** — с новыми фичами: `use(context)`, `<Context value={...}>`
-   **React Router v6** — вложенные и динамические маршруты, `Navigate`, `useLocation`
-   **TypeScript**
-   **Vite**
-   **json-server** — локальный API для разработки
-   **ESLint + Prettier** — контроль качества кода
-   **Context API** — управление состоянием авторизации
